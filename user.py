class User:
    def __init__(self,user_id):
        self.user_id = user_id
    """ need to have a dictonry of users
        users = {
            user_id:stuff,
            user_id:stuff,
            user_id:stuff,
        }
    """
print("test")
